export const lightTheme = {
  defaultBackgroundColor: "#ffffff",
  defaultColor: "#000000",
};

export const darkTheme = {
  defaultBackgroundColor: "#060606",
  defaultColor: "#FFFFFF",
};

export const theme = {
  lightTheme,
  darkTheme,
};

export default theme;
